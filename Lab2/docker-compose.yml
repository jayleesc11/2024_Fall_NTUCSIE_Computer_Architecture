services:
  peco:
    build:
      context: .
      dockerfile: dockerfile
    container_name: peco2
    volumes:
      - ./code:/root/project/code
      - ./testcases:/root/project/testcases
      - ./log:/root/project/log
