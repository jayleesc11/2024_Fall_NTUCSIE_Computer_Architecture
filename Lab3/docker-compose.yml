version: '3'
services:
  peco:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: peco3
    volumes:
      - ./code:/root/project/code
      - ./testcases:/root/project/testcases
      - ./log:/root/project/log
